"""الأساس المشترك الذي ترث منه نماذج قاعدة بيانات التطبيق."""
from sqlalchemy.orm import DeclarativeBase


class Base(DeclarativeBase):
    """
    يسجل metadata المشتركة التي تستخدمها نماذج السيرفر والتقارير والتحقيق.

    لا يحمل هذا الصف حالة مجال بنفسه؛ دوره توفير أصل موحد تستطيع منه كل
    النماذج إنشاء الجداول وربطها بالمحرك.
    """
    pass
